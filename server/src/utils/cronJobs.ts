export const setupCronJobs = (): void => {
  // Minimal stub for cron jobs. Implement scheduled tasks here.
  // This file exists to satisfy imports during type-check / build.
  return;
};

export default setupCronJobs;
